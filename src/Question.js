const Question = (question) => {
  return (
    <div className="Question">
      <p>Question</p>
      <div>
        <button>Yes</button>
        <button>No</button>
      </div>
    </div>
  );
};

export default Question;
